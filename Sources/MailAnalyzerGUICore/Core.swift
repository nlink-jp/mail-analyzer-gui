// MailAnalyzerGUICore — UI-independent logic layer. No AppKit imports allowed.
// Scaffold placeholder; the analyzer schema, subprocess contract, settings and
// promise-drop state machine land here module by module.
